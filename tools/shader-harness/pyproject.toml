[project]
name = "shader-harness"
version = "0.1.0"
description = "A small ModernGL-based harness for developing and testing GPU shaders using Python."
requires-python = ">=3.12"
dependencies = [
    "moderngl>=5.12.0",
    "pillow>=12.1.1",
    "pygame>=2.6.1",
]
